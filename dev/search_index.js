var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API-reference","page":"API","text":"","title":"API reference"},{"category":"section","location":"api/#Internal-helpers","page":"API","text":"","title":"Internal helpers"},{"category":"module","location":"api/#SpatialHAC","page":"API","text":"SpatialHAC\n\nSpatial HAC (Conley) standard errors for fitted linear mixed models at scale.\n\nComputes the Liang-Zeger GLS sandwich for the fixed effects of a MixedModels.LinearMixedModel, with a distance-decaying (Bartlett) kernel over same-period spatial pairs:\n\nVar(β̂) = B · M · B\nB = (X'Ω⁻¹X)⁻¹                       (GLS bread)\nM = A'(K ⊙ êê')A = Σᵢⱼ K(dᵢⱼ)·rᵢ·rⱼ'  (spatial meat)\nA = Ω⁻¹X ;  rᵢ = Aᵢ·êᵢ ;  ê = y − Xβ̂  (marginal residuals)\nΩ = I + WW',  W = ZΛ                  (from the fitted model)\n\nPoint estimates and Wald SEs are unchanged; only a spatially robust covariance of the fixed effects is added. Ω⁻¹ is applied via the Woodbury identity with one sparse Cholesky of I + W'W, so the method scales to millions of rows for nested random-effect designs.\n\nPitfall this package exists to avoid: applying Ω⁻¹ to the residuals (sᵢ = xᵢ·(Ω⁻¹ê)ᵢ) instead of the design is algebraically different and invalid for a GLS estimator — and the error is undetectable in the OLS limit, where both constructions coincide. The test suite anchors the formula against an independent dense definitional sandwich and the textbook OLS Conley.\n\nSee vcov_conley for the main entry point.\n\n\n\n\n\n","title":"SpatialHAC"},{"category":"function","location":"api/#SpatialHAC.vcov_conley","page":"API","text":"vcov_conley(m, lat, lon, period, cutoffs; kernel=:bartlett, check_tol=1e-6)\n    -> Vector{ConleyResult}\n\nSpatial-HAC (Conley) covariance of the fixed effects of a fitted LinearMixedModel. Case-weighted fits are supported: in the weight-whitened space the GLS sandwich is identical once X, the residuals and W = ZΛ are scaled by √w (done internally); for an unweighted fit √w = 1.\n\nArguments:\n\nm: fitted model.\nlat, lon: per-row coordinates in degrees, in the model's row order.\nperiod: per-row integer period (e.g. year); pairs are restricted to the same period (panel spatial-HAC convention).\ncutoffs: one or more cutoff distances, in km for :haversine or in the coordinate unit for :euclidean.\nkernel: :bartlett (1−u, default), :bartlett2 ((1−u)²), :uniform (1) or :epanechnikov (1−u²), with u = d/cutoff.\ndistance: :haversine (default; lat/lon in degrees, cutoff in km) or :euclidean (planar x/y in the same unit as the cutoff, e.g. projected metres/km). Use :euclidean when coordinates are already projected.\n\nPositive semi-definiteness. The estimator is PSD-admissible only when the kernel's Gram matrix is PSD for the coordinate dimension (Schoenberg class Pₚ; Kelejian & Prucha 2007). For 2-D coordinates this holds for :bartlett2 ((1−u)² ∈ P₂) but not for the linear :bartlett (1−u ∈ P₁ only), :uniform or :epanechnikov. When a non-PSD kernel produces a negative eigenvalue the covariance is eigenvalue-floored (floored=true) and a warning is issued; switch to :bartlett2 for a guaranteed-PSD estimator.\n\nAll cutoffs are computed in a single spatial sweep at the largest cutoff, threaded over periods. A runtime self-check verifies that the reconstructed Ω reproduces the model's own vcov(m) (tolerance check_tol) and errors otherwise, so silently wrong results are impossible.\n\nCaveats: the cutoff truncation makes this a LOWER BOUND on the spatial correction (report a cutoff-sensitivity curve); the candidate grid is not antimeridian-aware (pairs straddling ±180° longitude would be missed).\n\n\n\n\n\n","title":"SpatialHAC.vcov_conley"},{"category":"type","location":"api/#SpatialHAC.ConleyResult","page":"API","text":"ConleyResult\n\nResult for one cutoff: fields cutoff (km), vcov (p×p), se (Vector), coef (point estimates), names (coefficient names), n_pairs, min_eig (smallest pre-floor eigenvalue), floored::Bool, kernel::Symbol, distance::Symbol.\n\nImplements the StatsAPI accessors vcov, stderror, coefnames and coeftable; show prints a coefficient table.\n\n\n\n\n\n","title":"SpatialHAC.ConleyResult"},{"category":"function","location":"api/#SpatialHAC.vcov_cluster","page":"API","text":"vcov_cluster(m, cluster_id; type=:CR1, check_tol=1e-6) -> ClusterResult\n\nCluster-robust covariance of the fixed effects of a fitted LinearMixedModel, on the GLS estimand (same bread B = (X'Ω⁻¹X)⁻¹ as vcov_conley). The meat sums the per-cluster outer products of the row scores rᵢ = (Ω⁻¹X)ᵢ·êᵢ:\n\nM = Σ_g (Σ_{i∈g} rᵢ)(Σ_{i∈g} rᵢ)' ;   Var(β̂) = B · M · B\n\ncluster_id is a per-row label vector (any type, compared by equality). Sums are accumulated by cluster — no global N×N matrix is ever formed, so this scales to millions of rows and thousands of clusters.\n\ntype sets the small-sample factor (Cameron & Miller 2015; clubSandwich):\n\n:CR0  no correction (Liang & Zeger 1986 sandwich)\n:CR1  × m/(m−1)               (m = number of clusters)\n:CR1S × (m(N−1))/((m−1)(N−p)) (Stata's default)\n\nCase-weighted fits are supported (scores are formed in the weight-whitened space). The meat is PSD by construction, so no eigenvalue flooring is needed. A runtime self-check verifies the reconstructed Ω reproduces vcov(m).\n\n\n\n\n\n","title":"SpatialHAC.vcov_cluster"},{"category":"type","location":"api/#SpatialHAC.ClusterResult","page":"API","text":"ClusterResult\n\nResult of vcov_cluster: fields vcov (p×p), se (Vector), coef (point estimates), names (coefficient names), n_clusters, type (:CR0/:CR1/:CR1S), dof (residual degrees of freedom, N−p).\n\nImplements the StatsAPI accessors vcov, stderror, coefnames and coeftable; show prints a coefficient table.\n\n\n\n\n\n","title":"SpatialHAC.ClusterResult"},{"category":"function","location":"api/#SpatialHAC.suggest_cutoff","page":"API","text":"suggest_cutoff(m::LinearMixedModel, lat, lon, period; kwargs...) -> CovariogramResult\nsuggest_cutoff(ehat::AbstractVector, lat, lon, period; kwargs...) -> CovariogramResult\n\nData-driven Conley cutoff via the covariogram-range selector of Lehner (2026, arXiv:2603.03997): bin the products êᵢ·êⱼ of same-period residual pairs by great-circle distance, and select the first bin center at which the empirical covariogram Ĉ(h) crosses zero (tolerance eta, default 0). The selected range is used directly as the kernel cutoff — no kernel-dependent rescaling.\n\nFor a fitted model the marginal residuals ê = y − Xβ̂ are used — the same residuals that enter the vcov_conley meat.\n\nKeyword arguments (defaults follow the paper's recommendations):\n\nnbins = 150: number of distance bins (paper: 100-200);\nmax_frac = 2/3: bins span max_frac × the maximum same-period inter-point distance;\neta = 0.0: tolerance band; 0 selects the first sign change of Ĉ;\nmax_points = 10_000: rows are subsampled (without replacement, seeded rng) above this size — the covariogram is O(n²) in pairs. This is our adaptation for large panels; the paper benchmarks n ≈ 10⁴ without subsampling;\nrng = Xoshiro(2026): RNG for the subsample (fixed seed → reproducible).\n\nAdaptations vs. the paper (which is cross-sectional OLS): pairs are restricted to the same period (the same convention as vcov_conley), and residuals are the mixed-model marginal residuals.\n\nAlways inspect the returned covariogram and report a cutoff-sensitivity curve around ς̂: Lehner shows SE magnitude is inverse-U in the bandwidth, so neither tiny nor huge cutoffs are conservative.\n\n\n\n\n\n","title":"SpatialHAC.suggest_cutoff"},{"category":"type","location":"api/#SpatialHAC.CovariogramResult","page":"API","text":"CovariogramResult\n\nResult of the residual-covariogram cutoff selector (suggest_cutoff):\n\ncutoff: selected bandwidth ς̂ in km (NaN if the covariogram never reached the tolerance band within the binned domain);\nbins: distance-bin centers (km);\nC: empirical covariogram Ĉ(h_b) per bin (NaN for empty bins);\nn_pairs: number of same-period pairs per bin;\nn_used: rows retained after subsampling;\ncrossed: whether a zero crossing / tolerance hit was found.\n\n\n\n\n\n","title":"SpatialHAC.CovariogramResult"},{"category":"function","location":"api/#SpatialHAC.covariogram","page":"API","text":"covariogram(m, lat, lon, period; kwargs...) -> SpatialDiagnostic\ncovariogram(ehat, lat, lon, period; kwargs...) -> SpatialDiagnostic\n\nEmpirical covariogram Ĉ(h) of the (marginal) residuals: the binned mean of products êᵢ·êⱼ over same-period pairs at distance ≈ h (the same quantity the suggest_cutoff selector operates on, exposed as a diagnostic). Ĉ(h) starts near the residual variance at h→0 and decays toward 0.\n\nKeywords: nbins=150, max_frac=2/3 (bins span max_frac × max distance), max_points=10_000 (seeded subsample above this; pairs are O(n²)), rng=Xoshiro(2026), distance=:haversine (or :euclidean).\n\n\n\n\n\n","title":"SpatialHAC.covariogram"},{"category":"function","location":"api/#SpatialHAC.variogram","page":"API","text":"variogram(m, lat, lon, period; kwargs...) -> SpatialDiagnostic\nvariogram(ehat, lat, lon, period; kwargs...) -> SpatialDiagnostic\n\nEmpirical semivariogram γ̂(h) = ½·mean[(êᵢ−êⱼ)²] of the (marginal) residuals over same-period pairs at distance ≈ h (Matheron estimator). Note this returns γ (the semivariogram), not the variogram 2γ — matching gstat and GeoStats.jl. Under second-order stationarity γ(h) = Ĉ(0) − Ĉ(h) with Ĉ(0) the residual variance (sill); γ̂ rises from the nugget toward the sill.\n\nSame keywords as covariogram.\n\n\n\n\n\n","title":"SpatialHAC.variogram"},{"category":"type","location":"api/#SpatialHAC.SpatialDiagnostic","page":"API","text":"SpatialDiagnostic\n\nEmpirical spatial diagnostic of regression residuals (covariogram or variogram): fields kind (:covariogram or :semivariogram), h (bin centers), value (Ĉ(h) or γ̂(h) per bin, NaN for empty bins), n_pairs (per bin), n_used (rows after subsampling), distance.\n\n\n\n\n\n","title":"SpatialHAC.SpatialDiagnostic"},{"category":"function","location":"api/#SpatialHAC.scaled_re_matrix","page":"API","text":"scaled_re_matrix(m::LinearMixedModel) -> SparseMatrixCSC\n\nReconstruct W = Z·Λ from the public MixedModels API: sparse(rt) is the raw Z block (n × nlev·S) and rt.λ the S×S relative covariance factor, so Wᵢ = Zᵢ · kron(I_nlev, λᵢ).\n\n\n\n\n\n","title":"SpatialHAC.scaled_re_matrix"},{"category":"function","location":"api/#SpatialHAC._gls_bread_scores","page":"API","text":"_gls_bread_scores(m; check_tol=1e-6) -> (bread, S, n, p)\n\nShared GLS bread B = (X'Ω⁻¹X)⁻¹ and per-row scores S[i,:] = (Ω⁻¹X)ᵢ·êᵢ for the fixed-effects sandwich, used by both vcov_conley and vcov_cluster. Case weights are handled by whitening (X, ê, W = ZΛ scaled by √w). Runtime-validates that the reconstructed Ω reproduces vcov(m) and errors otherwise (closing the only silent-corruption channel).\n\n\n\n\n\n","title":"SpatialHAC._gls_bread_scores"},{"category":"function","location":"api/#SpatialHAC._binned_pairs","page":"API","text":"_binned_pairs(statfn, ehat, lat, lon, period; kwargs...) -> (h, value, counts, n_used)\n\nBin a per-pair statistic statfn(êᵢ, êⱼ) over same-period pairs by distance. statfn = (a,b)->a*b gives the covariogram; (a,b)->0.5*(a-b)^2 gives the semivariogram. Rows are subsampled (seeded) above max_points (pairs are O(n²)). Shared by suggest_cutoff, covariogram and variogram.\n\n\n\n\n\n","title":"SpatialHAC._binned_pairs"},{"category":"function","location":"api/#SpatialHAC.haversine_km","page":"API","text":"Great-circle distance in km (haversine).\n\n\n\n\n\n","title":"SpatialHAC.haversine_km"},{"category":"section","location":"estimator/#The-estimator","page":"The estimator","text":"","title":"The estimator"},{"category":"section","location":"estimator/#Setup","page":"The estimator","text":"A fitted linear mixed model has marginal covariance V = sigma^2 Omega with\n\nOmega = I + W W qquad W = Z Lambda\n\nwhere Z is the random-effects design and Lambda the (relative) covariance factor implied by the fitted variance components. The fixed effects solve the generalized-least-squares (GLS) estimating equation\n\nX Omega^-1 (y - Xhatbeta) = 0 ","title":"Setup"},{"category":"section","location":"estimator/#The-spatial-sandwich","page":"The estimator","text":"For the GLS estimator under an arbitrary true covariance operatornameCov(y) = Sigma,\n\noperatornameVar(hatbeta)\n  = B  big( XOmega^-1 Sigma Omega^-1 X big)  B\nqquad B = (XOmega^-1X)^-1\n\nFollowing Conley (1999) and the GEE logic of Liang & Zeger (1986), Sigma is estimated by a kernel-weighted outer product of the marginal residuals hat e = y - Xhatbeta:\n\nhatSigma_ij = K(d_ij) hat e_i hat e_j \n\nwith a kernel taper K(u), u = dc (default Bartlett 1-u; also (1-u)^2, uniform, Epanechnikov), pairs restricted to the same period (the panel spatial-HAC convention), and great-circle distances. The estimator is positive-semidefinite only for a kernel whose Gram matrix is PSD in the coordinate dimension (Schoenberg class P_p; Kelejian & Prucha, 2007, via Golubov 1981): in 2-D this holds for (1-u)^2 in P_2 but not for the linear Bartlett 1-u in P_1 only — hence the eigenvalue flooring. Writing A = Omega^-1 X and row scores r_i = A_i hat e_i,\n\nM = A(K odot hat ehat e)A = sum_ij K(d_ij) r_i r_j\nqquad\nwidehatoperatornameVar(hatbeta) = B M B \n\nsigma^2 cancels between bread and meat. Two limits anchor the construction: with Omega = I it reduces to the textbook OLS Conley estimator, and as c to 0 it reduces to the GLS analogue of HC0.","title":"The spatial sandwich"},{"category":"section","location":"estimator/#The-pitfall-this-package-exists-to-avoid","page":"The estimator","text":"It is tempting to form per-observation scores by applying Omega^-1 to the residuals: s_i = x_i (Omega^-1hat e)_i. This yields\n\nX operatornamediag(Omega^-1hat e) K operatornamediag(Omega^-1hat e) X\n\nwhich is not A(K odot hat ehat e)A and is invalid for a GLS estimator — in our application it inflated standard errors by an absurd ×60 and flipped a strongly significant coefficient to non-significance. Crucially, the two constructions coincide in the OLS limit (Omega = I), so OLS-based reduction tests cannot detect the error. The test suite therefore anchors the formula against an independent dense definitional sandwich (Omega formed explicitly, no Woodbury, no spatial grid) and against an independent R/lme4 reimplementation.","title":"The pitfall this package exists to avoid"},{"category":"section","location":"estimator/#Scalability","page":"The estimator","text":"Omega^-1 is never formed. With M_q = I + WW (size q times q, q = total number of random effects),\n\nOmega^-1 u = u - W M_q^-1 W u\n\n(Woodbury), requiring a single sparse Cholesky of M_q. For nested random-effect designs (e.g. pixel ⊂ glacier ⊂ region) the factor has nearly no fill-in: on a real 7.8M-row panel with q approx 425000, the factorization takes under a second and ~33 MB. Pair search uses an exact spatial-grid candidate filter (the haversine distance decides membership), all cutoffs are accumulated in a single sweep at the largest cutoff, and periods are processed in parallel threads.","title":"Scalability"},{"category":"section","location":"estimator/#Data-driven-cutoff-selection","page":"The estimator","text":"suggest_cutoff implements the covariogram-range selector of Lehner (2026, arXiv:2603.03997): bin the products hat e_i hat e_j of same-period residual pairs by distance (default ~150 bins spanning two-thirds of the maximum inter-point distance), and select the first bin center at which the empirical covariogram\n\nhat C(h) = frac1N(h) sum_(ij) in N(h) hat e_i hat e_j\n\ncrosses zero (Lehner's Eq. 5 with tolerance eta = 0). The selected range is used directly as the kernel cutoff — no kernel-dependent rescaling. In Lehner's Monte Carlo this selector controls the false-positive rate at or near the nominal level and outperforms every fixed-bandwidth alternative, with best size control under Bartlett and Epanechnikov kernels.\n\nTwo adaptations relative to the paper (which is cross-sectional OLS): pairs are restricted to the same period — the same panel convention as vcov_conley — and the residuals are the mixed-model marginal residuals hat e = y - Xhatbeta, i.e. exactly the residuals that enter the spatial meat. Above max_points rows (default 10,000) the selector works on a seeded random subsample, since the covariogram is quadratic in pairs; the paper benchmarks n approx 10^4 without subsampling.\n\nThe selector is validated in the test suite against an independent brute-force implementation of the binned covariogram and selection rule, and shown to recover a known spatial range on simulated spherical-covariance Gaussian fields.","title":"Data-driven cutoff selection"},{"category":"section","location":"estimator/#Practical-guidance","page":"The estimator","text":"Choose cutoffs from the data with suggest_cutoff, and report a sensitivity curve over several cutoffs around the selected value. Lehner (2026) shows the relationship between bandwidth and SE magnitude is inverse-U shaped: both too-narrow and too-wide bandwidths understate standard errors, so a deliberately huge cutoff is not a conservative choice.\nInterpret as a lower bound: correlation beyond the cutoff and across periods is not captured. Present these SEs alongside cluster-robust SEs at a level matched to your predictor's assignment scale (Moulton logic).\nThe result reports min_eig, floored and kernel. With 2-D coordinates, flooring can occur for the linear :bartlett, :uniform and :epanechnikov kernels (not PSD-guaranteed in 2-D); use :bartlett2 for a PSD-guaranteed estimator.","title":"Practical guidance"},{"category":"section","location":"validation/#Validation","page":"Validation","text":"The central claim of this package is not just that the estimators are implemented, but that they are validated — every estimator is anchored two independent ways: against a dense definitional reference (the formula re-implemented from scratch with none of the production shortcuts: no Woodbury identity, no spatial grid, no per-cluster accumulation), and where possible against an external cross-language/cross-package reference computed by code we did not write.","title":"Validation"},{"category":"section","location":"validation/#Why-this-discipline-exists","page":"Validation","text":"The first (pre-release) version of this estimator applied Ω⁻¹ to the residuals instead of the design matrix. Both constructions coincide exactly in the OLS limit (Ω = I), so OLS-reduction tests and internal-consistency tests (grid == brute force) all passed — while mixed-model standard errors were inflated by a factor of ~60. Only checks that anchor the formula — a dense definitional sandwich, and an independent re-implementation in another language — catch this class of error. Every estimator added since must pass both kinds of anchor before it ships; anything we cannot validate externally is cut (see the space×time HAC caveat in the README).","title":"Why this discipline exists"},{"category":"section","location":"validation/#Validation-matrix","page":"Validation","text":"Each row is enforced by the test suite (test/runtests.jl) on every CI run, except the cross-language rows, which are standalone scripts (CI runners lack R / heavy geostatistics dependencies) re-run manually before each release.\n\nCheck Independent reference Agreement Where\nMulti-cutoff production path Dense definitional sandwich (no Woodbury/grid) ~1e-14 runtests.jl\nOLS limit (Ω=I) Textbook Conley, brute double loop ~1e-15 runtests.jl\nDegenerate cutoff → 0 GLS-HC0 ~1e-16 runtests.jl\nCross-language (Conley) Independent R/lme4 re-implementation 1.8e-14 (matched θ) tests/julia (engine repo)\nCoverage Monte Carlo Spatial GP errors, 300 reps ≈90% vs 53% (Wald) engine repo\nWeighted models Dense weighted Liang-Zeger sandwich ~1e-14 runtests.jl\nUnit weights Unweighted fit (exact reduction) <1e-6 runtests.jl\nCluster-robust CR0/CR1/CR1S Dense block sandwich ~1e-15 runtests.jl\nCluster-robust cross-language R clubSandwich::vcovCR (lmer) 1.6e-4 (optimizer-limited) test/crosscheck_cluster.jl\nSingleton clusters GLS-HC0 (exact limit) <1e-7 runtests.jl\nKernels (4) Dense definitional sandwich per kernel ~1e-15 runtests.jl\nK₂ PSD in 2-D / uniform not Kernel Gram eigenvalues (Schoenberg P₂) exact runtests.jl\nEuclidean distance Dense planar sandwich ~1e-15 runtests.jl\nCovariogram selector Brute-force binned covariogram + selection rule exact runtests.jl\nRange recovery Spherical-GP field, known 25 km range within [0.5, 1.6]×R runtests.jl\nCovariogram / variogram Independent brute-force binning ~1e-12 runtests.jl\nSemivariogram identity γ = m₂ − Ĉ per bin (exact algebra) ~1e-12 runtests.jl\nVariogram cross-package GeoStatsFunctions.EmpiricalVariogram 6e-16 test/crosscheck_variogram_geostats.jl\nSoftware quality Aqua.test_all (11 checks) + JET.test_package pass runtests.jl","title":"Validation matrix"},{"category":"section","location":"validation/#The-runtime-self-check","page":"Validation","text":"Beyond the test suite, every call to vcov_conley / vcov_cluster validates itself at runtime: the Ω = I + WW' reconstructed from the model's random effects must reproduce the model's own vcov(m) to check_tol (default 1e-6),\n\nhatsigma^2 (XOmega^-1X)^-1 approx operatornamevcov(m)\n\notherwise the call errors instead of returning numbers. This closes the only silent-corruption channel — a wrong W = ZΛ reconstruction (e.g. after an internal MixedModels.jl change) — and, because the identity holds only for the correct √w-whitening, it also guards the weighted path against double-scaling.","title":"The runtime self-check"},{"category":"section","location":"validation/#A-worked-end-to-end-example","page":"Validation","text":"using MixedModels, SpatialHAC, DataFrames, CategoricalArrays, Random\nusing StatsAPI: coeftable\n\n# small synthetic spatial panel\nrng = Xoshiro(1)\nn = 600\ng = rand(rng, 1:15, n)\ndf = DataFrame(g = categorical(string.(\"g\", g)),\n               lat = 51.0 .+ 0.05 .* g .+ 0.01 .* randn(rng, n),\n               lon = -117.0 .- 0.07 .* g .+ 0.01 .* randn(rng, n),\n               yr = rand(rng, 2001:2003, n),\n               x = randn(rng, n))\ndf.y = 0.4 .+ 0.3 .* df.x .+ 0.5 .* sin.(g) .+ 0.6 .* randn(rng, n)\n\nm = fit(MixedModel, @formula(y ~ 1 + x + (1 | g)), df; progress = false)\n\n# diagnose the residual spatial structure, then select a cutoff\nvg  = variogram(m, df.lat, df.lon, df.yr; nbins = 30)\nsel = suggest_cutoff(m, df.lat, df.lon, df.yr; nbins = 30)\n\n# spatial-HAC and cluster-robust SEs on the same GLS estimand\nconley  = vcov_conley(m, df.lat, df.lon, df.yr, [5.0, 15.0, 30.0])\ncluster = vcov_cluster(m, df.g; type = :CR1)\n\ncoeftable(cluster)","title":"A worked end-to-end example"},{"category":"section","location":"#SpatialHAC.jl","page":"Home","text":"Spatial HAC (Conley) standard errors for linear mixed models at scale.\n\nSpatialHAC.jl computes spatially robust (Conley, 1999) standard errors directly on the fixed effects of a fitted MixedModels.jl model — the exact estimand of your mixed model, not an OLS surrogate. It scales to millions of observations via the Woodbury identity and a single sparse Cholesky factorization.\n\nTo our knowledge, no other package computes spatial-HAC standard errors on mixed models at this scale: conleyreg, fixest and acreg are OLS/GLM-only, while clubSandwich supports mixed models but is neither spatial nor scalable.","title":"SpatialHAC.jl"},{"category":"section","location":"#Why-you-might-need-this","page":"Home","text":"When observations close in space share unmodeled influences (weather, smoke plumes, shared coarse-resolution predictors), model-based (Wald) standard errors treat near-duplicate observations as independent evidence and become overconfident — often by large factors. SpatialHAC.jl corrects the uncertainty without changing your model or its point estimates: it replaces only the covariance of the fixed effects with a kernel-weighted sandwich in which pairs of nearby same-period observations are allowed to covary.","title":"Why you might need this"},{"category":"section","location":"#Installation","page":"Home","text":"using Pkg\nPkg.add(url = \"https://github.com/tofunori/SpatialHAC.jl\")","title":"Installation"},{"category":"section","location":"#Quick-start","page":"Home","text":"using MixedModels, SpatialHAC\n\nm = fit(MixedModel,\n        @formula(y ~ 1 + x + (1|group) + (1|unit) + (0 + x|group)),\n        df)\n\nres = vcov_conley(m, df.latitude, df.longitude, df.year,\n                  [5.0, 15.0, 25.0, 50.0])   # cutoffs in km\n\nfor r in res\n    @show r.cutoff r.se r.n_pairs r.floored\nend\n\nEach ConleyResult carries the cutoff, the robust covariance matrix, the standard errors, the number of in-range pairs, the smallest pre-floor eigenvalue and whether eigenvalue flooring was applied. All cutoffs are computed in one spatial sweep at the largest cutoff (each pair distance is computed once), threaded over periods — start Julia with julia -t auto.","title":"Quick start"},{"category":"section","location":"#Safety-by-construction","page":"Home","text":"vcov_conley self-validates at runtime: the covariance structure reconstructed from the fitted model must reproduce the model's own vcov(m) to a 1e-6 relative tolerance, otherwise the function refuses to return results. This closes the only silent-corruption channel (a wrong W = ZΛ reconstruction, for instance after an internal MixedModels.jl change).","title":"Safety by construction"},{"category":"section","location":"#Validation","page":"Home","text":"Check Independent reference Agreement\nMulti-cutoff production path Dense definitional sandwich (no Woodbury, no grid) ~1e-14\nOLS limit (Ω = I) Textbook Conley estimator, brute-force double loop ~1e-15\nDegenerate cutoff → 0 GLS-HC0 ~1e-16\nCross-language Independent R/lme4 reimplementation 1.8e-14 (matched θ)\nCoverage Monte Carlo Spatially correlated Gaussian-process errors ≈90% vs 53% (Wald)","title":"Validation"},{"category":"section","location":"#Caveats","page":"Home","text":"Lower bound. Cutoff truncation ignores residual correlation beyond the cutoff (large-group random effects, synoptic-scale fields) and across periods. Report a cutoff-sensitivity curve and cite these SEs alongside — not instead of — cluster-robust SEs.\nThe candidate grid is not antimeridian-aware: pairs straddling ±180° longitude are missed (the distances themselves are exact).\nThe estimated covariance structure is treated as known (standard GEE practice). Case-weighted fits are supported: the GLS sandwich is computed in the weight-whitened space (X, residuals and W = ZΛ scaled by √w).","title":"Caveats"}]
}
